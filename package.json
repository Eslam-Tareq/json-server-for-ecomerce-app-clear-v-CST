{
  "name": "json-server-vercel",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/server.js",
    "server": "npx json-server --watch newdb.json --port 3000"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0",
    "dotenv": "^17.3.1",
    "json-server": "^0.17.4",
    "mongodb": "^7.1.0"
  }
}
